# SkeinDB True Status Matrix

Last updated: 2026-05-11
Honest claims: SkeinDB does **not** provide and does **not** claim "100% MySQL compatibility." Coverage is measured against the regression corpus at `tests/compat/corpus.sql` (1658 lines / ~700+ statements) which is exercised on every commit by [`cluster_rpc::compat_corpus_statements`](../crates/skeindb/tests/cluster_rpc.rs). The surface that *is* supported is enumerated in [docs/MYSQL_COMPAT.md](./MYSQL_COMPAT.md). Anyone reading marketing copy claiming "100% MySQL parity" should treat that as false.

2026-05-11 (v0.3.15 R08 closure sync): Crate/package metadata is bumped to `0.3.15`. The research backlog is now **66 done / 43 open** after closing T280-T287. R08 now has persisted `view.create` definitions, column-granular dependency metadata, restricted filter/project/group-by incremental maintenance, auto full-refresh fallback, read-only `view.evaluate` oracle/benchmark reports (`skein.view.evaluate.v1`), and fixed SkeinAdmin Views refresh/evaluate controls. Compatibility also gains MySQL `information_schema.views` plus `information_schema.tables` VIEW rows and PostgreSQL `pg_catalog.pg_views` virtual catalog probes. `system.capabilities` now advertises 133 unique methods.

2026-05-11 (v0.3.14 R07 closure sync): Crate/package metadata is bumped to `0.3.14`. The research backlog is now **58 done / 51 open** after closing T270-T276. R07 now has runtime-tested write-write/dependency/constraint conflict hooks, executable values-only Wasm merge modules through the core sandbox, deterministic cancellation coverage, `merge.evaluate` workload reports (`skein.merge.evaluate.v1`), fixed SkeinAdmin Merge & CRDT controls, and offline write-queue documentation. Compatibility also gains MySQL `information_schema.table_privileges` and PostgreSQL `pg_catalog.pg_tables` virtual catalog probes. `system.capabilities` now advertises 132 unique methods.

2026-05-11 (v0.3.13 R06 closure sync): Crate/package metadata is bumped to `0.3.13`. The research backlog is now **51 done / 58 open** after closing T260-T266. R06 now has a documented SkeinForensic JSON filter grammar, chain-consistent index summaries, boundary hashes, checkpoint anchor proof metadata, Merkle roots and per-record inclusion proofs, self-describing `skein.forensic.bundle.v1` exports, fixed SkeinAdmin query/verify/export controls, and a simulated incident-timeline harness. The MySQL adoption surface also improves `information_schema.columns` with common ORM metadata fields such as `COLUMN_TYPE`, length/precision/scale, charset/collation, privileges, comments, generated expression, and `EXTRA`.

2026-05-11 (v0.3.12 R05 closure sync): Crate/package metadata is bumped to `0.3.12`. The research backlog is now **44 done / 65 open** after closing T250-T256. R05 now has a documented threat model and policy schema, persisted per-table `oblivious.policy.*` controls, padded scan/dummy lookup execution, `oblivious.explain`, the read-only `oblivious.evaluate` trace leakage/performance report, and SkeinAdmin Privacy controls wired to the typed runtime payloads. R18 and R19 remain prototype-level for the reasons listed below.

2026-05-09 (v0.3.11 R04 closure sync): Crate/package metadata is bumped to `0.3.11`. The research backlog is now **37 done / 72 open** after closing T240-T245. R04's DP aggregate path now has evidence for COUNT/SUM/AVG `dp.aggregate` payloads, bounded sensitivity metadata, per-principal persisted budgets, seeded Laplace/Gaussian noise, persisted budget-consumption audit events, and `privacy_etag` validators derived from DP privacy metadata and table versions. R18 and R19 remain prototype-level for the reasons listed below.

2026-05-09 (v0.3.10 DP evaluation sync): Crate/package metadata is bumped to `0.3.10`. The research backlog is now **31 done / 78 open** after closing T246. `dp.evaluate` runs bounded DP aggregate plans against exact baselines and seeded epsilon-grid noisy trials, returning mean/p95/max absolute error, mean relative error, noisy-query latency, and overhead-vs-exact metrics. SkeinAdmin's Privacy panel exposes the harness and now sends typed DP aggregate/budget/audit payloads. R18 and R19 remain prototype-level for the reasons listed below.

2026-05-09 (v0.3.9 replay CI sync): Crate/package metadata is bumped to `0.3.9`. The research backlog is now **30 done / 79 open** after closing T189. `skeindb replay run --json --out` emits replay run JSON artifacts, and `skeindb replay compare --baseline --candidate` compares base/head reports with threshold flags for p95/p99/span/storage/cache-hot-table deltas and fails non-zero on regressions. R18 remains prototype because T188 timing injection and cache/LSM reconstruction fidelity are still open.

2026-05-09 (v0.3.8 admin help-center release): Crate/package metadata is bumped to `0.3.8`. SkeinAdmin gains a dedicated **Help & Docs** panel with a 5-step quick-start, a panel-reference table that one-click jumps into every panel, an R01-R20 research-track index with hardness pills and primary RPC methods, keyboard shortcut and deep-link reference, glossary, support links, and a live filter search; the panel is wired from the left nav, top tab strip, the topbar `? Help` button, and the `?` keyboard shortcut. The new surface is locked by `skeinadmin_help_panel_exposes_comprehensive_documentation_center`. The research backlog status is unchanged at **29 done / 80 open** with 18 hardened / 2 prototype tracks; this release does not claim any new research-track closures.

2026-05-09 (R14 replay redaction sync): The research backlog is now **29 done / 80 open** after closing T185. `maintenance.replay.export` accepts optional primary-key redaction (`none`, `hash_pk`, `drop_pk`), stores redaction metadata in the bundle, computes correctness/performance checksums after redaction, and exposes the option through SkeinAdmin and `skeindb replay export`. This is primary-key redaction only; non-key payload columns remain in replay artifacts.

2026-05-09 (R18 performance replay sync): The research backlog is now **28 done / 81 open** after closing T187. Replay bundles can carry optional `skein.replay.performance.v1` profiles with storage/LSM counters, cache warm hints, timing summaries, and profile checksums; `maintenance.replay.run` returns `performance_report` variance deltas for annotated bundles. R18 remains prototype because timing injection, cache/LSM reconstruction fidelity, and CI latency-distribution gates are still open.

2026-05-09 (R19 ABI sync): The research backlog is now **27 done / 82 open** after closing T084 with columnar batch ABI/data interchange evidence. `wasm.plan.compile` now returns artifact metadata, `wasm.plan.inspect` validates and describes artifacts, `wasm.plan.edge_package` emits a host-backed manifest/JS runner, and SkeinAdmin's Wasm panel uses the current query/artifact API. R19 remains prototype because native Wasm codegen, SIMD exploration, and standalone edge execution are still open.

2026-05-09 (v0.3.7 release-prep sync): Crate/package metadata is bumped to `0.3.7` and public docs/site status strings are synced after the R12/R17/R20 hardening batch. The research backlog was **26 done / 83 open** at release-prep time; this version is a progress release, not a claim that the full research backlog is finished. The remaining prototype tracks were still R18 and R19 until their code/test/docs evidence lands.

2026-05-08 (truth sync + review): The core project backlog now has **140 done / 0 open** checked roadmap tasks; remaining product work is captured as partial-phase hardening notes, future PostgreSQL/driver compatibility follow-ups, and the separate research backlog. Runtime review of the high-risk MVCC, WAL recovery, Wasm UDF, encryption, RowDir, PostgreSQL SQL rewrite/dispatch, stats, replay, history, and compaction surfaces found no high-confidence correctness or security fixes. One hygiene fix landed in `system.capabilities`: the advertised SkeinQL method list no longer duplicates `cdc.subscribe_query`, and `crates/skeindb/src/server.rs::system_capabilities_method_list_is_unique` now locks the canonical advertised count at 127 unique methods. The PostgreSQL startup log now says trust/SCRAM auth instead of the stale trust/cleartext wording.

2026-05-08 (research R01/R03 closure): The research backlog now closes R01 T232-T235 and R03 T073-T076 with existing runtime/test evidence rather than new unchecked claims. R01 is promoted to **Hardened**: `ValueStoreConfig.enable_learned_index`, `ValueStore::get_with_trace`, `ModelRefreshPolicy`, `ValueStore::benchmark`, and the learned-index tests cover feature-flagged hybrid lookup, fallback degradation, refresh triggers, p50/p99/p99.9 probe benchmarks, and distribution-shift behavior. R03 remains **Hardened** with its checklist reconciled: `DeltaPolicy.snapshot_interval`, geometric skip patches, `ValueStore::compact_deltas`, `DeltaCompactionReport.energy`, `ValueStore::delta_benchmark`, and `topology_analysis()` are covered by focused ValueStore tests.

2026-05-08 (research R20 closure): R20 is promoted to **Hardened** with an executable energy-aware compaction slice. `maintenance.compaction.set_policy` now accepts `policy: "energy_aware"`, persisted energy coefficients, and `external_signals` for power source, battery percentage, price, and carbon multipliers. `stats.snapshot.compaction.scheduler.energy` and `maintenance.compaction.status` expose CPU/IO joule estimates, signal multiplier, constraint score, and deferral ratio. SkeinAdmin includes R20 controls for those signals, and `eval/compaction_scheduler_dashboard.py` compares `energy_aware` against fixed/workload policies with energy score and p99 latency summaries.

2026-05-08 (research R17 + security closure): R17 is promoted to **Hardened**. `migration.intent_report` now has pattern evidence for pagination, polling, soft deletes, hierarchy self-joins, recursive CTEs, EXISTS membership, and COALESCE defaults; `migration.rewrite_preview` emits before/after SkeinQL-native migration snippets; and the new read-only `migration.report_export` endpoint returns both JSON and Markdown reports for offline review. SkeinAdmin's Migration panel calls the exporter, and `crates/skeindb/src/engine.rs::migration_report_export_contains_json_and_markdown` locks the output shape. The two open Dependabot alerts for `wasmtime` CVE-2026-44216 / GHSA-p8xm-42r7-89xg are addressed by upgrading `skeindb-core` to `wasmtime` 43.0.2 and disabling the memory64 proposal in the UDF sandbox config.

2026-05-08 (research R12 closure): R12 is promoted to **Hardened** with a deterministic NL-to-SkeinQL verification baseline. `ai.nl.translate` packages schema-aware prompts and emits rule-based SkeinQL for simple read requests; `ai.nl.explain` derives tables/projection/filter/order/limit summaries, dependency sets, preview rows, and approval tokens; `ai.nl.execute` rechecks the token against query+args+deps before executing. SkeinAdmin's NL Lab exposes the prompt, editable query JSON, preview, approval, and execution flow, and `skeindb nl-eval` reports exact and execution-match metrics. Evidence: `ai_nl_translate_explain_execute_roundtrip`, `ai_nl_translate_explain_execute_rpc_roundtrip`, and `eval_examples_uses_rule_translation_for_execution_match`.

2026-04-26 (research T230/T231): R01 learned-index instrumentation now exports ValueID lookup distributions instead of only keeping internal counters. `ValueStore::lookup_distribution()` returns a 256-bucket prefix histogram, non-empty bucket count, top hot buckets, and model-vs-current L1 shift when a learned-index baseline exists. `stats.snapshot.storage.value_lookup` exposes the same histogram for admin/runtime tooling. T231 adds `ValueStore::learned_index_report()` for the offline-built piecewise model: segment count, bounded search window, sampled segment coefficients, model bytes, fallback entries, and fallback byte estimates. Focused coverage: `crates/skeindb-core/tests/valuestore.rs::lookup_distribution_exports_histogram_and_top_buckets`, `crates/skeindb-core/tests/valuestore.rs::learned_index_report_describes_offline_model_and_fallback`, and `crates/skeindb/src/server.rs::stats_snapshot_reports_dedup_metrics`.

2026-04-25 (v0.3.5 dashboard wiring): `stats.snapshot` now surfaces the previously-stub fields the dashboard was rendering as "--". `EngineStorageStats` is extended with `total_rows`, `total_tables`, `disk_bytes` (computed by walking the data directory), `mvcc_versions` (sum of per-row version counters), and `delta_chains` (live `ValueStore::stats().delta_values_written`). New top-level JSON sections `mvcc.{versions, delta_chains}` and `cache.{hit_pct, size_bytes, hits, misses}` (the latter sourced from `Counters.plan_cache_hits/misses`) are emitted by `stats_snapshot`. A real `etag_hits` counter on `Counters` increments at both `If-None-Match` paths in `GET /api/v1/q/{query_id}` so `query.etag_hits` reflects 304 responses instead of always being 0. `sessions.active` and `connections` now report `open_txns` instead of hardcoded zero. Dashboard cards Total Rows / Total Tables / Disk / MVCC Versions / Delta Chains / Cache Hit % / ETag Hits all show real data.

2026-04-25 (v0.3.4 admin polish): SkeinAdmin's Overview and Security panels are now wired end-to-end. Dashboard cards `Top Tables`, `Slow Query Log`, `Active Sessions`, and `Index Health` now read live data via `information_schema.tables` (`sql.exec`), `stats.slow_queries`, and `stats.snapshot` through new `silentRpc` / `unwrapCellValue` helpers. Three response-shape bugs in the Security panel were fixed (`securityCreateToken` / `securityRefreshTokens` / `securityTopQueries` were reading `r?.secret` / `r?.tokens` / `r?.queries` instead of `r.json.result.*`); create-token flow no longer overwrites the freshly-generated secret with the token list refresh. Overview/Security panels now auto-refresh on tab switch. Active Sessions card relabeled to match `stats.snapshot` fields (`Sessions` / `Open Txns` / `Avg Latency`). Asset coverage in `crates/skeindb/tests/skeinadmin_assets.rs` (5 tests) and 62 cluster RPC tests still green.

2026-04-24: SkeinAdmin Easy Viewer ships a **WYSIWYG schema editor** (Design tab). It loads a table via `schema.describe_table`, lets the operator rename / retype / mark nullable / set default / drop columns inline, computes an `ALTER TABLE` plan (`ADD/DROP/RENAME/MODIFY/CHANGE COLUMN`) by diffing original vs draft, previews the plan in-place, and applies statements one at a time via `sql.exec` (halts on first error). Asset coverage in `crates/skeindb/tests/skeinadmin_assets.rs::skeinadmin_easy_design_tab_exposes_wysiwyg_schema_editor`. Backend ergonomics: `skeindb version` now prints a richer banner with format and dialect doc pointers; new `skeindb info --data ./data [--json]` subcommand prints catalog/storage/port summary for ops use; `skeindb serve` now prints a startup banner (data dir, storage mode, HTTP/SkeinAdmin/MySQL/PostgreSQL/QUIC/cluster URLs) before binding. `Engine::storage_mode_name()` is now a public read-only accessor.

2026-04-24 (encryption, clustering, PostgreSQL/admin/distribution hardening): Phase 20 dedup-preserving encryption now ships its first end-to-end operator surface — `EncryptedValueStore` (`skeindb-core::encrypted_valuestore`) layers encrypt-on-write / decrypt-on-read over the existing `ValueStore` without changing the on-disk `.vseg` format (envelopes are stored as ordinary `ValueKind::Cell` blobs and parsed back through the strict `EncryptionEnvelope::from_stored_bytes` reader); `DatabaseKeyManager::rotate_active_key` returns a `KeyRotationPlan` and pairs with `reencrypt_envelope` / `EncryptedValueStore::reencrypt_value` plus a `ReencryptionProgress` counter set; and the engine now exposes `settings.encryption.status` / `set_mode` / `register_key` / `set_active_key` / `rotate_key` JSON-RPC methods (master key bytes accepted as base64 but **never persisted to disk** — operators re-register on restart) with a redacted 256-entry in-memory audit ring and a dedicated SkeinAdmin `Encryption` panel (sidebar + top tab). T191/T192/T193 are now closed in the backlog with focused coverage in `crates/skeindb-core/tests/encryption.rs` and `crates/skeindb/tests/skeinadmin_assets.rs`. Phase 14/17 cluster movement now computes shard-scoped object manifests from live row versions, preflights destination ValueID coverage with `objects.need`, pulls only missing objects via `objects.pull` before changing placement, and returns manifest/progress summaries from `cluster.shard.move` / `cluster.shard.rebalance`; MySQL COM_STMT prepare metadata now covers supported projection-level scalar subqueries plus embedded scalar-subquery arithmetic, `serve` now defaults row persistence to `segment`/`.rseg`, the PostgreSQL listener now handles common startup/bootstrap probes including `current_database()`, `current_schema()`, `SHOW server_version` / `server_version_num` / `standard_conforming_strings` / `max_identifier_length`, `SHOW transaction isolation level`, and `SELECT current_setting(...)`, Phase 25 now also exposes virtual `pg_catalog` coverage (`pg_database`, `pg_namespace`, `pg_type`, `pg_proc` stub, `pg_settings`, `pg_stat_activity`) through both `sql.exec` and the live PG listener, SkeinAdmin’s live surface was reconciled with the runtime (`settings.list`, telemetry/security navigation, research-config hydration, cluster leave / user revoke, Easy Viewer inline DB creation plus create-table validation/preview, the Forensics panel’s `maintenance.audit_*` controls, a dedicated CDC subscriptions page with lag visualization, a dedicated Time Travel & Replay page with `query.select as_of`, `maintenance.history.*`, and `maintenance.replay.*` workflows, and live compaction pressure/workload cards backed by `stats.snapshot`), Phase 21 now includes both reproducible scheduler evaluation artifacts and live scheduler policy controls (`maintenance.compaction.*`, `stats.snapshot.compaction.scheduler`, and hard-pressure write backpressure for write-classified SkeinQL/HTTP mutations), and Phase 26 now adds Debian packaging metadata, a signed apt publication path, and a repo-scoped Homebrew tap formula with tag-driven updates.

Phase 19 now also exposes snapshot-based replay bundles via `maintenance.replay.export` / `maintenance.replay.import` / `maintenance.replay.run` and `skeindb replay export|verify|run`, materializing deterministic verification workspaces under `.replay_workspaces` with canonical checksums over captured schema+row snapshots plus retained change-event metadata. 2026-04-23 release-candidate hardening: `ENC_MLE_DB` in `skeindb-core::encryption` no longer uses a fixed AEAD nonce; the nonce is now HKDF-derived from the database master secret plus the plaintext digest in the same scope as the content key, keeping convergent dedup while removing the fixed-nonce review finding. Replay export now rejects reversed LSN ranges (`from_lsn > to_lsn`) with `invalid_request`, and `maintenance.replay.import` / `maintenance.replay.run` now bound `workspace_id` length to at most 128 ASCII chars in addition to the existing character-set check. The docs-site architecture figure path was corrected so `crates/skeindb/tests/docs_site_assets.rs::architecture_figure_path_resolves` is green again and `cargo test --workspace` passes end-to-end.

This matrix reconciles runtime reality with backlog checklists.

Interpretation:
- **Implemented**: shipped in runtime and exercised by tests.
- **Partial / Prototype**: usable surfaces exist, but hardening/completeness tasks remain open.
- **Planned**: backlog intent exists, but no meaningful runtime implementation yet.

## 1) Backlog checklist snapshot

- `docs/PROJECT_BACKLOG.md`: **140 done / 0 open** (140 top-level roadmap tasks; remaining product gaps are tracked as partial-phase hardening notes and future compatibility/research work rather than unchecked core-roadmap boxes)
- `docs/RESEARCH_BACKLOG.md`: **66 done / 43 open** (109 total)

Why `RESEARCH_BACKLOG` mostly remains open: those checklists now represent
publication-grade hardening/evaluation tasks; prototype runtime coverage is tracked below.

## 2) Core roadmap (Phases 0-26)

| Phase | Current status | Notes / evidence |
|---|---|---|
| Phase 0 Repo setup | Implemented | T001/T002/T003 are complete in runtime and tests: VarU/CRC/value ID primitives, FileHeader read/write, and RecordFrame append/iterate (`crates/skeindb-core/src/lib.rs`, `crates/skeindb-core/tests/phase0_format.rs`). |
| Phase 1 Storage core | Partial | Prototype engine persists JSON state and includes adaptive ValueID-backed row ref encoding for table files (`format_version: 2`, emits `"$skein_ref"` only when byte-profitable); full MANIFEST/WAL/LSM pipeline remains open. |
| Phase 2 SQL + metadata | Partial | Catalog + minimal CREATE/INSERT/SELECT paths are implemented (`engine`, `sql.exec`), including virtual `information_schema.tables` and `information_schema.columns` with ORM-facing metadata (`COLUMN_TYPE`, length/precision/scale, charset/collation, privileges, comments, generated expression, and `EXTRA`), plus the bounded PostgreSQL-oriented `sql.exec` rewrite layer (`CREATE/DROP SCHEMA`, `search_path`, `current_schema()`, `current_database()`, `version()`, selected `current_setting(...)`, `SERIAL`/`BIGSERIAL`/`SMALLSERIAL`, inline `PRIMARY KEY`/`UNIQUE`, `DROP DATABASE`, and `CREATE INDEX IF NOT EXISTS`). |
| Phase 3 MySQL protocol | Implemented (baseline) | Wire listener performs handshake + `mysql_native_password` auth exchange and supports `COM_QUERY` through the SQL-translation subset (`SELECT/SHOW/USE/CREATE DATABASE/DROP DATABASE/CREATE TABLE/CREATE [UNIQUE] INDEX/ALTER TABLE .../DROP INDEX/DROP TABLE/TRUNCATE TABLE/RENAME TABLE/INSERT/INSERT ... SELECT/UPDATE/DELETE`, `INSERT IGNORE`, `REPLACE`, `INSERT ... ON DUPLICATE KEY UPDATE`, multi-table `DELETE`, multi-table `UPDATE` (implemented), `UNION`/`UNION ALL`, `DISTINCT`, `BETWEEN`/`NOT BETWEEN`, `REGEXP`/`RLIKE`/`NOT REGEXP`, `<=>` (NULL-safe equality), simple joins including `INNER`/`LEFT`/`RIGHT`/`CROSS JOIN`/`NATURAL JOIN`/`FULL OUTER JOIN` with multi-join chains, comma-separated `FROM` lists, `JOIN ... USING (...)`, derived tables (FROM subqueries), CTEs (`WITH...AS`), projection aliases, wildcard projections, aggregate shims for `COUNT(*)`/`COUNT(col)`/`COUNT(DISTINCT col)`/`SUM`/`MIN`/`MAX`/`AVG`/`GROUP_CONCAT()`/`BIT_AND()`/`BIT_OR()`/`BIT_XOR()` with `HAVING`/`ORDER BY`/`LIMIT`/`OFFSET`, window functions (`ROW_NUMBER()`/`RANK()`/`DENSE_RANK()` with `OVER(PARTITION BY ... ORDER BY ...)`), `SET @var = ...`/`SELECT @var` user variables, non-aggregate `GROUP BY` de-dup, broad scalar-function coverage including `DEGREES`/`RADIANS`/`PERIOD_ADD`/`PERIOD_DIFF`/`MAKEDATE`/`MAKETIME`/`CONCAT_WS`/`REPEAT`/`REVERSE`/`LPAD`/`RPAD`/`SPACE`/`HEX`/`UNHEX`/`FORMAT`/`SIGN`/`SQRT`/`POW`/`TRUNCATE`/`LOG`/`LN`/`LOG2`/`LOG10`/`EXP`/`PI`/`RAND`/`UUID`/`SLEEP`/`BENCHMARK`/`FIELD`/`ELT`/`INET_ATON`/`INET_NTOA`/`BIN`/`OCT`/`CONV`/`CRC32`/`MD5`/`SHA1`/`SHA`/`SHA2` plus the prior scalar baseline, JSON functions (`JSON_EXTRACT`/`JSON_UNQUOTE`/`JSON_OBJECT`/`JSON_ARRAY`/`JSON_CONTAINS`/`JSON_LENGTH`/`JSON_TYPE`/`JSON_VALID`/`JSON_SET`/`JSON_KEYS`/`JSON_MERGE_PRESERVE`), date/time functions including `STR_TO_DATE`/`WEEK`/`YEARWEEK`/`CONVERT_TZ`/`UTC_TIMESTAMP`/`UTC_DATE`/`UTC_TIME`/`SYSDATE`/`ADDTIME`/`SUBTIME`/`TIME_TO_SEC`/`SEC_TO_TIME` plus the prior date/time baseline, session functions `USER()`/`CURRENT_USER()`/`SESSION_USER()`/`SYSTEM_USER()`/`LAST_INSERT_ID()`/`CONNECTION_ID()`, `CASE`/`CAST`, arithmetic expressions, subquery rewrites, `SQL_CALC_FOUND_ROWS`/`FOUND_ROWS()`, `SELECT ... FOR UPDATE`/`FOR SHARE`/`LOCK IN SHARE MODE` locking hint stripping, `EXPLAIN` stub, `DO` statement, `CREATE VIEW`/`DROP VIEW` stubs, `SAVEPOINT`/`RELEASE SAVEPOINT`/`ROLLBACK TO SAVEPOINT` stubs, `SET GLOBAL` no-ops, `FLUSH`/`ANALYZE`/`OPTIMIZE`/`CHECK`/`REPAIR TABLE` no-ops, `KILL` no-op). Wire protocol includes `COM_INIT_DB` (0x02) and `COM_STATISTICS` (0x09). `SHOW` coverage expanded with `SHOW CREATE DATABASE`, `SHOW WARNINGS`/`ERRORS`, `SHOW PROCESSLIST`/`FULL PROCESSLIST`, `SHOW TRIGGERS`/`EVENTS`/`PROCEDURE STATUS`/`FUNCTION STATUS`, `SHOW PLUGINS`/`PROFILES`. `INFORMATION_SCHEMA` now includes `tables`/`columns`/`schemata`/`statistics` plus the newer compatibility virtual tables (`key_column_usage`, `table_constraints`, `character_sets`, `collations`, `engines`, `routines`, `triggers`, `views`, `processlist`, `user_privileges`, `table_privileges`). The rest of the prior baseline (column defaults, `ALTER TABLE` variants, index enforcement, prepared-statement support, compatibility `SET`/`SHOW` forms, `LOCK TABLES`/`UNLOCK TABLES` no-ops, `NULL`-as-unknown semantics, secondary-index enforcement with `1062`/`23000` wire errors) is unchanged. `tests/compat/corpus.sql` runs end-to-end over the MySQL listener in integration tests. Corpus expanded to 1657 lines with about 678 semicolon-terminated SQL statements. Latest: correlated subqueries in projection, binary comparison operators (`>`, `<`, `>=`, `<=`, `=`, `!=`, `<>`) in scalar expression parser, multi-aggregate GROUP BY with ORDER BY over JOINs, embedded subquery pre-evaluation in arithmetic, expression-based UPDATE SET with per-row evaluation (`data_update_exprs` engine method), WordPress Site Health-style `information_schema.TABLES` storage summaries, `information_schema.table_privileges` ORM probes, and dedicated MySQL-wire WordPress installer seed regressions for escaped single-row `INSERT`s, trailing `/* LOCK */` comments, serialized transient option upserts, and `TABLE_NAME IN (...)` plus `ENGINE = ...` probes. COM_STMT column flags (NOT_NULL/PRIMARY_KEY/UNSIGNED/NUM/BINARY/AUTO_INCREMENT) are emitted per column in prepared-statement result metadata; `mysql_stmt_expr_flags()` + `mysql_stmt_resolve_column_flags()` infer flags from expression types and table descriptors. Type metadata extended with 50+ new function name mappings for prepared-statement type inference, and prepare-time metadata now also covers supported projection-level scalar subqueries plus embedded scalar-subquery arithmetic via dedicated unit + MySQL-wire regressions. All Phase 3 backlog items complete. |
| Phase 4 Web console | Partial (advanced) | `/api/v1/sql/exec` HTTP endpoint is live; console scaffold + schema/sql workspace exist (`web/skeinadmin`); CSV + JSON export/import supported with `resultToCSV()`/`parseCSV()` functions; format selector in data browse tab. `admin.user.*` RPCs (create/list/drop/grant/revoke) with `DbUser` struct; admin UI wired to user management RPCs. |
| Phase 5 SkeinQL API | Implemented (baseline) | Typed SkeinQL models + `/api/v1/rpc` + `schema.*` (list/create/describe/drop) + `query.select` + `tx.begin/tx.commit/tx.rollback` are implemented. |
| Phase 6 ETag cache coherence | Implemented (baseline) | `data.get` ETag / `If-Match` updates + dependency-aware query paths; `query.prepare` + `GET /api/v1/q/{query_id}` with ETag/If-None-Match (304 Not Modified); SSE subscription via `query.subscribe` + `/api/v1/q/:query_id/events` with broadcast-based change notifications on mutations; integration tests T062+T063 in `cluster_rpc.rs`. |
| Phase 7 Delta values | Implemented (prototype) | DELTA kind, policy/metrics, and compaction behavior are implemented in ValueStore tests. |
| Phase 8 Wasm extensions | Implemented (baseline) | Wasm module catalog, scalar UDF sandboxing, fuel/time cancellation, aggregate UDF execution, and table-function UDF execution are implemented with focused `skeindb-core` tests. Wasm query operators remain a separate prototype track under R19. |
| Phase 9 Audit WAL | Implemented (baseline) | Forensic hash chain upgraded to BLAKE3-256 via `audit_hash256`; format v3 with `CheckpointAnchor` records plus persisted `last_verified_ms`; `maintenance.audit_status` + `maintenance.audit_verify` RPCs; anchors auto-written at checkpoint; SkeinAdmin now surfaces audit status/verify alongside filtered `forensic.query`, proof-backed `forensic.verify`, and `skein.forensic.bundle.v1` export tooling with boundary, checkpoint-anchor, and Merkle inclusion proofs. |
| Phase 10 Row/column snapshots | Partial | Snapshot build/read/incremental paths exist; optimizer coverage still evolving. |
| Phase 11 Compat telemetry + migration | Implemented (baseline) | `telemetry.feature_flags`, `telemetry.compat_summary`, `telemetry.migration_hints`, and `telemetry.workload_features` endpoints; `observe_mysql_sql_features()` records 20+ feature categories (DML/joins/aggregates/windows/subqueries/CTE/JSON/DDL/transactions/session variables/prepared stmts); `migration.*` intent/rewrite surfaces; integration test in `cluster_rpc.rs::telemetry_and_plan_cache_integration`. |
| Phase 12 SkeinAdmin | Implemented (baseline) | SkeinAdmin is embedded and functional; 26 feature center cards; research dashboard with 18 hardened / 2 prototype status badges; Easy Viewer with row numbers, NULL display, quick-filter, type-aware formatting, inline SQL tab, Ctrl+Enter shortcut, column tooltips, improved insert form, required-field validation, inline database creation, create-table validation + live SQL preview, column sorting (click-to-sort headers), styled modal confirmations, search operator dropdown (LIKE/=/ !=/>/</BETWEEN/IS NULL/IS NOT NULL/REGEXP), visual query builder tab (column picker, WHERE conditions, ORDER BY/LIMIT, SQL preview, execute/copy/send), dialect-aware SQL profiles/workspace hints for SkeinQL/MySQL/PostgreSQL modes, 5 new dashboard cards (Top Tables, Slow Query Log, Active Sessions, Index Health, Research Track Status); telemetry/plan cache/coalescing dashboard cards; Settings manager with `settings.list`, preset keys, and method explorer; Security panel with API token create/list/revoke UI + modal confirmations + top queries by fingerprint panel; CDC panel with subscribe/poll/ack/close flows plus session-local lag bars and recent-event inspection; Time Travel & Replay panel with `query.select as_of`, `maintenance.history.*` retention controls, replay bundle export/download/import, and integrity summaries from `maintenance.replay.run`; Forensics panel with `maintenance.audit_status` / `maintenance.audit_verify` plus forensic query/export tooling; the Migration panel now calls `migration.report_export` for JSON/Markdown report bundles; the NL Lab exposes prompt packaging, preview/explain, approval-token capture, and gated execution; cluster leave and user revoke controls are exposed in the main UI. |
| Phase 13 Observability | Partial | `stats.snapshot`, `stats.top_queries`, `stats.slow_queries`, and `/metrics` exist, including live dedup storage metrics (`dedup_ratio`, logical/unique/duplicate bytes); deeper latency histogram surfaces remain open. |
| Phase 14 Cluster scale-out | Implemented (baseline) | Node identity, fanout replication, shard metadata, cluster RPCs, CAS object pull protocol (`objects.need`/`objects.missing`/`objects.fetch` RPCs with `ValueStore::contains()` Bloom-ready check), manifest-backed shard movement (`cluster.shard.manifest`, `cluster.shard.move`, `cluster.shard.rebalance`) with destination preflight + progress summaries, read-only replica write rejection via `enforce_cluster_write_guard()`, and `cluster.route_query` RPC for read-balancing hints are all implemented. |
| Phase 15 Perf improvements | Implemented (baseline) | T150-T153 are closed: interned-column schema flags, ValueID-first predicate operations, late materialization, batch scan/filter/project execution, and the MVCC `VisibleVersionIndex` cache are implemented with focused unit/integration coverage. |
| Phase 16 Query coalescing | Implemented (baseline) | `QueryCoalescer` with leader/follower pattern; coalescing active for GET /api/v1/q/{query_id} and `query.patch` (conditional); `stats.coalescing` endpoint with leader/follower counters, in-flight count, saved_executions metric; fingerprint canonicalization via `query_fingerprint()`. |
| Phase 17 CAS-aware replication | Implemented (baseline) | `objects.need`/`objects.missing`/`objects.fetch` RPCs implemented; replica-side `objects.pull` now batches locally missing ValueIDs, pulls lossless VE1 payloads, verifies fetched objects against requested ValueIDs before import, and recursively fetches missing delta bases. Shard movement now reuses the same CAS path through shard-scoped object manifests, destination `objects.need` preflight, and progress reporting on `cluster.shard.move` / `cluster.shard.rebalance`. `ValueStore::contains()` provides O(1) existence checks; `BloomFilter` uses BLAKE3 double-hashing for membership and backs `bloom_maybe_contains()`/`bloom_stats()`. |
| Phase 18 Index advisor | Partial (advanced) | `advisor.index_synthesize/apply_index/history/dismiss` are live, `telemetry.workload_features` extracts predicate/order_by/group_by/join_key columns from SQL workload, candidate synthesis suppresses PK/existing-index prefixes plus previously applied or dismissed suggestions, and SkeinAdmin now renders ranked suggestions/history with observed-before/expected-after scan reports; online build progress and rollback remain open. |
| Phase 19 Time travel + replay | Implemented (baseline) | MVCC `as_of` reads, SQL compatibility controls, history retention/GC, snapshot-based replay bundle export/import/run, and the SkeinAdmin Time Travel & Replay page are implemented. `maintenance.replay.export` emits typed replay bundles with schema snapshots, retained row versions, filtered change-event metadata, per-table checksums, a manifest checksum, optional primary-key redaction, and optional performance profiles; `maintenance.replay.import` materializes bundles into hidden `.replay_workspaces/<workspace_id>` roots; `maintenance.replay.run` reopens the imported workspace and verifies canonical checksums. CLI coverage includes `skeindb replay export`, `skeindb replay verify`, and `skeindb replay run`; SkeinAdmin now surfaces point-in-time query execution via `query.select as_of`, history status/policy/GC, replay bundle export/download/import, redaction controls, session-local replay workspace tracking, and checksum/integrity summaries. Replay remains intentionally snapshot-based rather than full WAL mutation re-execution. |
| Phase 20 Encryption | Shipped baseline | `skeindb-core::encryption` provides database-scoped key registration, active key selection, `EncryptionContext` / `EncryptionEnvelope` wrappers, and AEAD helpers for `ENC_RANDOM` (randomized AES-256-GCM-SIV nonces) and `ENC_MLE_DB` (HKDF-derived content key + nonce from `(master_key, plaintext_digest)`). T191 adds `EncryptedValueStore` (`skeindb-core::encrypted_valuestore`) — an envelope-as-`ValueKind::Cell` wrapper with `put_encrypted` / `get_decrypted` / `read_envelope` / `reencrypt_value` that does **not** alter the on-disk `.vseg` format. T192 adds `DatabaseKeyManager::rotate_active_key` returning a `KeyRotationPlan`, `reencrypt_envelope` with a `ReencryptionProgress` counter set, and value-store-level `EncryptedValueStore::reencrypt_value`. T193 exposes `settings.encryption.status` / `set_mode` / `register_key` / `set_active_key` / `rotate_key` JSON-RPC methods (master keys are accepted as base64 but never persisted) plus a SkeinAdmin `Encryption` panel and a redacted in-memory audit ring. Operator-driven background re-encryption pass + on-disk key catalog persistence remain follow-ups. |
| Phase 21 Compaction scheduler | Partial | `stats.snapshot` now carries both live telemetry and `compaction.scheduler` policy state (configured/effective budgets, peak-window activation, task priority, and write-admission mode), `maintenance.compaction.status/set_policy/pause/resume` persist and expose the runtime scheduler surface, safe mode applies write backpressure to write-classified SkeinQL/HTTP requests at hard L0 bounds, and `eval/compaction_scheduler_dashboard.py` still provides deterministic stall-rate and p99-latency comparisons; deep background-compactor execution remains heuristic/reporting-oriented rather than a full autonomous worker. |
| Phase 22 Autoparam + plan cache | Implemented (baseline) | `ai.autoparam.classify/analyze` with rule-based literal classification; `plan_cache.status` returns cache entries with hit counts, fingerprints, creation/last-hit timestamps; `plan_cache.clear` clears select + patch caches; `CachedSelect` enriched with query/hits/created_ms/last_hit_ms/schema_version; `SET @@skein.autoparameterize = 1` MySQL session variable support; SkeinAdmin top queries by fingerprint display; integration test in `cluster_rpc.rs::telemetry_and_plan_cache_integration`. |
| Phase 23 CDC/changefeeds | Partial | `cdc.subscribe_table` and `cdc.subscribe_query` are live, the retained change log now stamps table-level events with `commit_ts_ms` plus `lsn`-style sequence metadata, `cdc.poll`/`cdc.ack`/`cdc.close` maintain consumer cursors and return explicit `resnapshot_required` metadata when a client falls behind the retained horizon, and `GET /api/v1/cdc/sse/:sub_id` streams both data events and `resnapshot` control events with bounded replay batches plus `Last-Event-ID`/`from_offset` reconnect semantics; SkeinAdmin exposes the CDC panel with session-local subscription management and lag visualization. WebSocket transport and durable cursors remain open. |
| Phase 25 PostgreSQL compat | Partial (advanced) | T400-T418 are closed: PG v3 framing/startup/SSL rejection/listener, trust mode plus SCRAM-SHA-256 when `SKEINDB_TOKEN` is set, session settings with `SET`/`RESET`/`SHOW`/`current_setting`, SQL dialect rewrites, PG DDL/DML compatibility including `ON CONFLICT` rewrites and `RETURNING` follow-up reads where supported, type OID inference plus binary result encoding, expanded virtual `pg_catalog` tables (`pg_database`, `pg_namespace`, `pg_tables`, `pg_views`, `pg_class`, `pg_attribute`, `pg_type`, `pg_index`, `pg_constraint`, `pg_proc` stub, `pg_settings`, `pg_stat_activity`), startup probes, extended query protocol, transaction/savepoint state, SQLSTATE mapping, PG corpus coverage, and unit/integration tests. It is still not full PostgreSQL compatibility: COPY protocol, partial portal suspension, broader dialect/catalog parity, and production-grade driver/framework matrices remain follow-up work. |
| Phase 26 Distribution | Partial | The repo now carries `cargo-deb` metadata for `skeindb`, a repo-local `Formula/skeindb.rb` tap entry for Homebrew, and a tag-driven GitHub Actions workflow that builds a source tarball plus `.deb`, updates the formula, and can publish a signed apt repository to the `apt` branch once the signing secrets are configured. |

## 3) Research tracks (R01-R20)

| Track | Runtime status | Primary runtime surface |
|---|---|---|
| R01 Learned indexes | Hardened | ValueStore learned index scaffolding plus exported lookup-distribution histograms and offline model reports (`ValueStore::lookup_distribution`, `ValueStore::learned_index_report`, `stats.snapshot.storage.value_lookup`), feature-flagged hybrid learned+fallback lookup (`ValueStoreConfig.enable_learned_index`, `ValueStore::get_with_trace`), refresh policy (`ModelRefreshPolicy`, `should_refresh`, `refresh_learned_index`), benchmark probe quantiles (`ValueStore::benchmark`), and distribution-shift fallback tests in `skeindb-core/valuestore` plus `server::stats_snapshot_reports_dedup_metrics`. |
| R02 Adaptive row/column | Hardened | Snapshot surfaces and hybrid execution scaffolds; integration test creates table, inserts rows, triggers `system.snapshot`, verifies data readable after (`cluster_rpc.rs::r02_adaptive_storage_format_selection`). |
| R03 Delta topology | Hardened | Delta-chain policy/skip/compaction paths + `topology_analysis()` with depth stats (avg/max/p50/p99), fanout, hot-chain detection, savings ratio. |
| R04 Differential privacy | Hardened | `dp.*` endpoints + COUNT/SUM/AVG bounded aggregates, persisted budget/audit, privacy-aware `privacy_etag` validators, `dp.evaluate` accuracy-vs-epsilon and overhead-vs-exact reports, crypto-quality hash-based PRNG (`DpRng`), and Rényi DP composition tracking (`rdp_alphas`, `query_count`, `rdp_gaussian_cost()`, `rdp_laplace_cost()`, `rdp_to_eps_delta()`). |
| R05 Oblivious execution | Hardened | `oblivious.policy.*`, `oblivious.explain`, and `oblivious.evaluate`; threat-model docs, per-table policy persistence, padded scans, dummy ValueStore lookups, deterministic trace leakage metrics, overhead reports, SkeinAdmin Privacy controls, engine/RPC coverage, and `cluster_rpc.rs::r05_oblivious_padding_verification`. |
| R06 Forensic WAL queries | Hardened | `forensic.query`, `forensic.verify`, `forensic.export`; SkeinForensic JSON filter grammar, time/table/op/actor index summaries, boundary hashes, checkpoint-anchor proof metadata, Merkle tree roots (`forensic_merkle_root()`), per-record inclusion proofs (`forensic_merkle_proof()`), exportable `skein.forensic.bundle.v1` reports, tamper detection, incident-timeline test coverage, and SkeinAdmin Forensics query/verify/export wiring. |
| R07 Client-side merge funcs | Hardened | `merge.register`, `merge.apply`, `merge.simulate`, `merge.evaluate`, and `merge.wasm.*`; write-write/dependency/constraint conflict hooks, values-only Wasm execution through the core sandbox, fuel/time cancellation coverage, offline queue spec, and SkeinAdmin Merge & CRDT controls. Evidence: `merge_apply_wasm_policy_executes_values_only_module`, `merge_apply_wasm_policy_cancels_non_terminating_module`, `merge_evaluate_reports_conflict_rate_and_success`, `merge_apply_wasm_policy_executes_rpc`, `skeinadmin_merge_panel_exposes_r07_hardening_controls`, and `cluster_rpc.rs::r07_merge_conflict_resolution_deterministic`. |
| R08 Incremental views | Hardened | `view.create/drop/refresh/evaluate/status/explain_deps`; restricted single-table filter/project/group-by views; persisted dependency usage in `views.json` format v2; grouped incremental maintenance by touched-group recompute over persisted source rows; auto refresh falls back to full recompute when change breadth exceeds grouped/view cardinality thresholds; `view.evaluate` compares cloned incremental refresh against full recompute and reports timing/speedup; BFS-based cascading invalidation through view-on-view deps; `view_dependency_graph()` with `ViewDepEdge` structs; transitive dep traversal in `view_explain_deps`; SkeinAdmin Views evaluation controls and MySQL/PG view catalog probes. |
| R09 QUIC-native protocol | Hardened | QUIC transport + integration tests (`tests/quic_rpc.rs`); multi-stream sequential RPCs + rebind verification test (`quic_rpc.rs::r09_quic_concurrent_multi_stream_rpcs`). |
| R10 Vector embeddings | Hardened | `vector.insert/search/index.status`; HNSW graph index (M=16, M_max0=32, ef_construction=64) with insert/search/prune; automatic index rebuild on insert; cosine/dot/L2 metrics; falls back to brute-force when filter is active. |
| R11 LLM/autoparam | Hardened | `ai.autoparam.classify/analyze`; integration test classifies 2 literals from real SQL query and verifies labels count, then analyzes full SQL and verifies normalized_sql/fingerprint/literals extraction (`cluster_rpc.rs::r11_llm_autoparam_classify_and_analyze`). |
| R12 NL -> SkeinQL | Hardened | `ai.nl.translate/explain/execute`, schema-aware prompt packages, dependency-backed explanations, preview rows, approval-token-gated execution, `skeindb nl-eval`, and engine/RPC/eval coverage. |
| R13 Causal ETag consistency | Hardened | ETag/min-causality controls; V2 vector-clock format (`CAUSALITY_FORMAT_V2`); `merge_causality_tokens()` (component-wise max); `causality_dominates()` (partial order); `ensure_min_causality()` accepts V1+V2. |
| R14 Replay bundles | Hardened | `edge.bundle.request/apply/status`; `maintenance.replay.export` primary-key redaction (`none`, `hash_pk`, `drop_pk`) before checksum/performance metadata; integration tests cover edge bundle request/apply/status and redacted replay export/import/run (`cluster_rpc.rs::r14_geo_replay_bundle_roundtrip`, `cluster_rpc.rs::t183_replay_bundle_export_import_run_roundtrip`). |
| R15 Schema evolution | Hardened | `schema.propose_change/merge_status/apply_merge`; integration test creates table, proposes add_column change, checks merge_status by change_id, applies merge (`cluster_rpc.rs::r15_schema_evolution_propose_merge_apply`). |
| R16 Auto index synthesis | Hardened | `advisor.index_synthesize/apply_index/history/dismiss` plus the SkeinAdmin Index Advisor page with ranked suggestions/history and observed-before/expected-after reports; engine coverage verifies candidate suppression when a MySQL-compatible prefix index already exists, `advisor.apply_index` now queues background builds with queued/building/completed/failed history state and rollback metadata, and integration tests assert both successful apply/dismiss round-trips and rollback-on-failure resurface behavior (`cluster_rpc.rs::r16_index_advisor_synthesis_workflow`, `cluster_rpc.rs::r16_index_advisor_apply_roundtrip_and_suppresses_suggestion`, `cluster_rpc.rs::r16_index_advisor_apply_failure_rolls_back_and_resurfaces_suggestion`). |
| R17 Intent inference | Hardened | `migration.intent_report`, `migration.rewrite_preview`, and `migration.report_export`; pattern library covers pagination, polling, soft deletes, hierarchy self-joins, recursive CTEs, EXISTS membership, and COALESCE defaults with JSON/Markdown report export coverage. |
| R18 Perf regression replay | Prototype implemented | `maintenance.replay.export/import/run` now support optional performance profiles with storage/cache/timing sections and replay variance reports; timing injection, cache/LSM reconstruction fidelity, and CI distribution comparison remain open. |
| R19 Wasm query operators | Prototype implemented | `wasm.plan.compile/inspect/edge_package/run`, host-interpreted plan artifacts, batch ABI/data interchange, and SkeinAdmin query/artifact controls. Native Wasm codegen and SIMD remain open. |
| R20 Energy-aware compaction | Hardened | `energy_aware` compaction policy, CPU/IO joule estimates, persisted external energy signals, status/stats JSON, SkeinAdmin controls, safe-mode constraint preservation, and synthetic energy-vs-p99 evaluation harness. |

## 4) Recommended “next truth-maintenance” rule

When a task is promoted from prototype to hardened behavior, update:
1. The corresponding checkbox in `docs/PROJECT_BACKLOG.md` or `docs/RESEARCH_BACKLOG.md`.
2. This matrix row (status + evidence pointer).
3. At least one test reference proving the claim.
