{
  "name": "skeinadmin",
  "private": true,
  "version": "0.0.1",
  "description": "SkeinAdmin — framework-free management console for SkeinDB (served embedded at /admin and /console).",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "npx http-server -p 5174 .",
    "build": "echo 'Static console: no build step required (vanilla HTML/CSS/ES modules).'"
  }
}
