feat: column flags, corpus expansion, CSV, CAS pull, replica router (T037/T038/T043/T142/T143)

Phase 3 — MySQL protocol:
- T037: COM_STMT column flags — NOT_NULL/PRIMARY_KEY/UNIQUE_KEY/UNSIGNED/BINARY/
  AUTO_INCREMENT/NUM constants, mysql_stmt_expr_flags() with ~30 scalar function
  mappings, mysql_stmt_resolve_column_flags() for table descriptors, grouped
  aggregate flag pass-through
- T038: corpus.sql expanded from ~298 to ~400 statements — numeric/math, string
  extended, bit/hash, date/time extended, JSON, CTE, subqueries, UNION, derived
  tables, NULL-safe equality, REGEXP/RLIKE, EXPLAIN, maintenance no-ops, SHOW
  variants, system variables, SET GLOBAL. COM_QUERY FOR UPDATE/FOR SHARE/LOCK IN
  SHARE MODE stripping. SOUNDEX() scalar function. @@max_allowed_packet variable.
  Fixed doubled-quote ('') handling in find_matching_parenthesis.

Phase 4 — Web console:
- T043: CSV export/import — resultToCSV() with escaping, parseCSV() with quoted
  fields, wired into admin console exportData()/importData() handlers

Phase 14 — Cluster management:
- T142: CAS object pull — objects.need/missing/fetch RPCs, parse_value_id(),
  ValueStore::contains() O(1) check, base64-encoded fetch with integrity verify
- T143: Read-only replica router — cluster.route_query RPC with read balancing
  hints (standalone/write_primary/read_replica/read_primary), round-robin replica
  selection, write rejection enforcement

All 251 tests pass (177 unit + 23 integration + 51 other).
