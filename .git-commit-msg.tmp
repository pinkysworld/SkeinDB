feat: telemetry + plan cache + corpus expansion + admin WYSIWYG overhaul

Rust runtime:
- Telemetry (Phase 11): feature_flags, compat_summary, migration_hints
  endpoints with 48 MySQL feature definitions and auto-observation
- Plan cache (Phase 22): plan_cache.status/clear with hit counts and
  fingerprint tracking
- Query coalescing (Phase 16): stats.coalescing with leader/follower
  metrics
- R11/R14/R15 integration tests: autoparam classify, geo replay bundle,
  schema evolution propose/merge/apply
- New telemetry_and_plan_cache_integration test

corpus.sql (490 lines, ~298 SQL statements):
- Uncommented 16+ TODO blocks (IF/NULLIF, EXISTS, REGEXP, CAST,
  COUNT DISTINCT, window functions, CTEs, RIGHT/CROSS JOIN, derived
  tables, NOT EXISTS, IN/scalar subquery, multi-table DELETE/UPDATE,
  LOCK/UNLOCK, INFORMATION_SCHEMA, nested functions, SHOW variants)
- Added ~60 new SQL statements: JOINs, INSERT...SELECT, DO, EXPLAIN,
  SHOW variants, system variables, CREATE/DROP VIEW, SAVEPOINT,
  GROUP_CONCAT DISTINCT, session functions, SET GLOBAL, scalar funcs
- Re-commented 5 statements exposing parser limitations

admin console (web/skeinadmin):
- Column sorting with server-side ORDER BY
- skeinModal() replaces all confirm() calls
- Search operator dropdown (11 operators)
- Visual query builder tab with SQL preview
- 5 new dashboard cards (Top Tables, Slow Query Log, Active Sessions,
  Index Health, Research Track Status)

docs: MYSQL_COMPAT, PROJECT_BACKLOG, TRUE_STATUS_MATRIX, RESEARCH_BACKLOG,
  site/index.html updated

All 244 tests pass (cargo fmt + clippy + test).
